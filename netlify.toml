[build]
  command = "npm install" # Or any other necessary build/setup command
  functions = "netlify/functions"
  publish = "" # Leave this empty, as it's not required for server deployments

[[redirects]]
from = "/api/*"
to = "/.netlify/functions/server"
status = 200